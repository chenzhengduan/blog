# 前端性能优化 - Frontend Performance Optimization

## 1. 页面加载性能优化 - Page Load Performance Optimization

### 场景对话：讨论首屏加载时间优化

**English:**
```
Interviewer: "How would you optimize the initial page load time for a large React application?"

Candidate: "I'd approach this systematically using several strategies:

**Code Splitting and Lazy Loading**:
```javascript
// Route-based code splitting
import { lazy, Suspense } from 'react';
import { Routes, Route } from 'react-router-dom';

const Dashboard = lazy(() => import('./pages/Dashboard'));
const Profile = lazy(() => import('./pages/Profile'));
const Settings = lazy(() => import('./pages/Settings'));

function App() {
  return (
    <Suspense fallback={<div>Loading...</div>}>
      <Routes>
        <Route path="/dashboard" element={<Dashboard />} />
        <Route path="/profile" element={<Profile />} />
        <Route path="/settings" element={<Settings />} />
      </Routes>
    </Suspense>
  );
}

// Component-level lazy loading
const HeavyChart = lazy(() => 
  import('./components/HeavyChart').then(module => ({
    default: module.HeavyChart
  }))
);

function Analytics() {
  const [showChart, setShowChart] = useState(false);
  
  return (
    <div>
      <button onClick={() => setShowChart(true)}>
        Load Chart
      </button>
      {showChart && (
        <Suspense fallback={<ChartSkeleton />}>
          <HeavyChart />
        </Suspense>
      )}
    </div>
  );
}
```

**Resource Optimization**:
```javascript
// Image optimization with next/image
import Image from 'next/image';

function ProductCard({ product }) {
  return (
    <div>
      <Image
        src={product.image}
        alt={product.name}
        width={300}
        height={200}
        priority={product.featured} // LCP optimization
        placeholder="blur"
        blurDataURL="data:image/jpeg;base64,/9j/4AAQSkZJRgABAQAAAQ..."
        sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
      />
    </div>
  );
}

// Font optimization
// In _document.js or HTML head
<link
  rel="preload"
  href="/fonts/inter-var.woff2"
  as="font"
  type="font/woff2"
  crossOrigin="anonymous"
/>

// CSS optimization
<link rel="preload" href="/styles/critical.css" as="style" />
<link rel="stylesheet" href="/styles/critical.css" />
<link
  rel="preload"
  href="/styles/non-critical.css"
  as="style"
  onLoad="this.onload=null;this.rel='stylesheet'"
/>
```

**Bundle Optimization**:
```javascript
// Webpack configuration for optimization
module.exports = {
  optimization: {
    splitChunks: {
      chunks: 'all',
      cacheGroups: {
        vendor: {
          test: /[\\/]node_modules[\\/]/,
          name: 'vendors',
          chunks: 'all',
        },
        common: {
          name: 'common',
          minChunks: 2,
          chunks: 'all',
          enforce: true,
        },
      },
    },
    usedExports: true,
    sideEffects: false,
  },
  resolve: {
    alias: {
      'lodash': 'lodash-es', // Use ES modules for better tree shaking
    },
  },
};

// Tree shaking optimization
// Instead of importing entire library
import _ from 'lodash'; // ❌ Imports entire library

// Import only what you need
import { debounce, throttle } from 'lodash'; // ✅ Better
import debounce from 'lodash/debounce'; // ✅ Best
```

**Critical Resource Prioritization**:
```html
<!-- Resource hints for performance -->
<head>
  <!-- DNS prefetch for external domains -->
  <link rel="dns-prefetch" href="//fonts.googleapis.com">
  <link rel="dns-prefetch" href="//api.example.com">
  
  <!-- Preconnect for critical third-party origins -->
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  
  <!-- Preload critical resources -->
  <link rel="preload" href="/api/user" as="fetch" crossorigin>
  <link rel="preload" href="/images/hero.webp" as="image">
  
  <!-- Prefetch likely next page resources -->
  <link rel="prefetch" href="/dashboard">
</head>
```

The results typically show:
- **40-60% reduction** in initial bundle size
- **2-3 second improvement** in First Contentful Paint
- **Lighthouse score increase** from 60 to 90+
- **Better user experience** with progressive loading"
```

**中文对照：**
```
面试官："你如何优化大型React应用的初始页面加载时间？"

候选人："我会系统性地使用几种策略来解决这个问题：

**代码分割和懒加载**：
```javascript
// 基于路由的代码分割
import { lazy, Suspense } from 'react';
import { Routes, Route } from 'react-router-dom';

const Dashboard = lazy(() => import('./pages/Dashboard'));
const Profile = lazy(() => import('./pages/Profile'));
const Settings = lazy(() => import('./pages/Settings'));

function App() {
  return (
    <Suspense fallback={<div>加载中...</div>}>
      <Routes>
        <Route path="/dashboard" element={<Dashboard />} />
        <Route path="/profile" element={<Profile />} />
        <Route path="/settings" element={<Settings />} />
      </Routes>
    </Suspense>
  );
}
```

**资源优化**：包括图片优化、字体优化、CSS优化等

**打包优化**：代码分割、摇树优化、别名配置等

**关键资源优先级**：DNS预取、预连接、预加载、预取等

通常结果显示：
- **初始包大小减少40-60%**
- **首次内容绘制改善2-3秒**
- **Lighthouse分数从60提升到90+**
- **渐进式加载带来更好的用户体验**"
```

### 相关词汇
- **First Contentful Paint (FCP)** /fɜːrst ˈkɑːntentfəl peɪnt/ - 首次内容绘制
- **Largest Contentful Paint (LCP)** /ˈlɑːrdʒəst ˈkɑːntentfəl peɪnt/ - 最大内容绘制
- **Time to Interactive (TTI)** /taɪm tuː ˌɪntərˈæktɪv/ - 可交互时间
- **Code splitting** /koʊd ˈsplɪtɪŋ/ - 代码分割
- **Lazy loading** /ˈleɪzi ˈloʊdɪŋ/ - 懒加载
- **Tree shaking** /triː ˈʃeɪkɪŋ/ - 摇树优化
- **Bundle size** /ˈbʌndl saɪz/ - 包大小
- **Critical path** /ˈkrɪtɪkl pæθ/ - 关键路径
- **Resource hints** /ˈriːsɔːrs hɪnts/ - 资源提示
- **Preload** /ˈpriːloʊd/ - 预加载
- **Prefetch** /ˈpriːfetʃ/ - 预取
- **DNS prefetch** /diː en es ˈpriːfetʃ/ - DNS预取
- **Preconnect** /ˈpriːkənekt/ - 预连接
- **Progressive loading** /prəˈɡresɪv ˈloʊdɪŋ/ - 渐进式加载
- **Skeleton screen** /ˈskelətən skriːn/ - 骨架屏

## 2. 运行时性能优化 - Runtime Performance Optimization

### 场景对话：React性能优化策略

**English:**
```
Interviewer: "What strategies do you use to optimize React component performance and prevent unnecessary re-renders?"

Candidate: "I use several React-specific optimization techniques:

**Memoization Strategies**:
```javascript
// React.memo for component memoization
const ExpensiveComponent = React.memo(({ data, onUpdate }) => {
  const processedData = useMemo(() => {
    return data.map(item => ({
      ...item,
      computed: heavyComputation(item)
    }));
  }, [data]);
  
  const handleClick = useCallback((id) => {
    onUpdate(id);
  }, [onUpdate]);
  
  return (
    <div>
      {processedData.map(item => (
        <ItemComponent 
          key={item.id}
          item={item}
          onClick={handleClick}
        />
      ))}
    </div>
  );
}, (prevProps, nextProps) => {
  // Custom comparison function
  return (
    prevProps.data.length === nextProps.data.length &&
    prevProps.data.every((item, index) => 
      item.id === nextProps.data[index].id &&
      item.version === nextProps.data[index].version
    )
  );
});

// useMemo for expensive calculations
function DataVisualization({ rawData, filters }) {
  const filteredData = useMemo(() => {
    return rawData
      .filter(item => filters.categories.includes(item.category))
      .filter(item => item.date >= filters.startDate)
      .sort((a, b) => b.value - a.value);
  }, [rawData, filters.categories, filters.startDate]);
  
  const chartConfig = useMemo(() => {
    return {
      data: filteredData,
      options: {
        responsive: true,
        plugins: {
          legend: { position: 'top' },
          title: { display: true, text: 'Data Analysis' }
        }
      }
    };
  }, [filteredData]);
  
  return <Chart {...chartConfig} />;
}

// useCallback for stable function references
function TodoList({ todos, onToggle, onDelete }) {
  const [filter, setFilter] = useState('all');
  
  const handleToggle = useCallback((id) => {
    onToggle(id);
  }, [onToggle]);
  
  const handleDelete = useCallback((id) => {
    onDelete(id);
  }, [onDelete]);
  
  const filteredTodos = useMemo(() => {
    switch (filter) {
      case 'active':
        return todos.filter(todo => !todo.completed);
      case 'completed':
        return todos.filter(todo => todo.completed);
      default:
        return todos;
    }
  }, [todos, filter]);
  
  return (
    <div>
      <FilterButtons filter={filter} onFilterChange={setFilter} />
      {filteredTodos.map(todo => (
        <TodoItem
          key={todo.id}
          todo={todo}
          onToggle={handleToggle}
          onDelete={handleDelete}
        />
      ))}
    </div>
  );
}
```

**Virtual Scrolling for Large Lists**:
```javascript
// Using react-window for virtualization
import { FixedSizeList as List } from 'react-window';

function VirtualizedList({ items }) {
  const Row = ({ index, style }) => (
    <div style={style}>
      <ItemComponent item={items[index]} />
    </div>
  );
  
  return (
    <List
      height={600}
      itemCount={items.length}
      itemSize={80}
      width="100%"
    >
      {Row}
    </List>
  );
}

// Custom virtual scrolling implementation
function CustomVirtualList({ items, itemHeight = 50 }) {
  const [scrollTop, setScrollTop] = useState(0);
  const containerHeight = 400;
  
  const visibleStart = Math.floor(scrollTop / itemHeight);
  const visibleEnd = Math.min(
    visibleStart + Math.ceil(containerHeight / itemHeight) + 1,
    items.length
  );
  
  const visibleItems = items.slice(visibleStart, visibleEnd);
  
  return (
    <div
      style={{ height: containerHeight, overflow: 'auto' }}
      onScroll={(e) => setScrollTop(e.target.scrollTop)}
    >
      <div style={{ height: items.length * itemHeight, position: 'relative' }}>
        {visibleItems.map((item, index) => (
          <div
            key={visibleStart + index}
            style={{
              position: 'absolute',
              top: (visibleStart + index) * itemHeight,
              height: itemHeight,
              width: '100%'
            }}
          >
            <ItemComponent item={item} />
          </div>
        ))}
      </div>
    </div>
  );
}
```

**State Management Optimization**:
```javascript
// Context optimization with multiple contexts
const UserContext = createContext();
const ThemeContext = createContext();
const NotificationContext = createContext();

// Instead of one large context
const AppContext = createContext(); // ❌ Causes unnecessary re-renders

// Split contexts by update frequency
function AppProvider({ children }) {
  const [user, setUser] = useState(null);
  const [theme, setTheme] = useState('light');
  const [notifications, setNotifications] = useState([]);
  
  return (
    <UserContext.Provider value={{ user, setUser }}>
      <ThemeContext.Provider value={{ theme, setTheme }}>
        <NotificationContext.Provider value={{ notifications, setNotifications }}>
          {children}
        </NotificationContext.Provider>
      </ThemeContext.Provider>
    </UserContext.Provider>
  );
}

// Zustand for efficient state management
import { create } from 'zustand';
import { subscribeWithSelector } from 'zustand/middleware';

const useStore = create(
  subscribeWithSelector((set, get) => ({
    user: null,
    theme: 'light',
    notifications: [],
    
    setUser: (user) => set({ user }),
    setTheme: (theme) => set({ theme }),
    addNotification: (notification) => 
      set((state) => ({ 
        notifications: [...state.notifications, notification] 
      })),
  }))
);

// Component only re-renders when specific state changes
function UserProfile() {
  const user = useStore((state) => state.user);
  const setUser = useStore((state) => state.setUser);
  
  // This component won't re-render when theme or notifications change
  return <div>{user?.name}</div>;
}
```

These optimizations typically result in:
- **50-70% reduction** in unnecessary re-renders
- **Smooth 60fps** performance for large lists
- **Improved user interaction** responsiveness
- **Better memory usage** patterns"
```

**中文对照：**
```
面试官："你使用什么策略来优化React组件性能并防止不必要的重新渲染？"

候选人："我使用几种React特定的优化技术：

**记忆化策略**：
- React.memo用于组件记忆化
- useMemo用于昂贵的计算
- useCallback用于稳定的函数引用

**大列表的虚拟滚动**：
- 使用react-window进行虚拟化
- 自定义虚拟滚动实现

**状态管理优化**：
- 拆分Context以减少不必要的重新渲染
- 使用Zustand等高效的状态管理库

这些优化通常带来：
- **不必要重新渲染减少50-70%**
- **大列表保持流畅的60fps性能**
- **改善用户交互响应性**
- **更好的内存使用模式**"
```

### 相关词汇
- **Memoization** /ˌmeməˈzeɪʃn/ - 记忆化
- **Re-render** /riː ˈrendər/ - 重新渲染
- **Virtual scrolling** /ˈvɜːrtʃuəl ˈskroʊlɪŋ/ - 虚拟滚动
- **Virtualization** /ˌvɜːrtʃuələˈzeɪʃn/ - 虚拟化
- **Callback** /ˈkɔːlbæk/ - 回调函数
- **Dependency array** /dɪˈpendənsi əˈreɪ/ - 依赖数组
- **Shallow comparison** /ˈʃæloʊ kəmˈpærəsn/ - 浅比较
- **Deep comparison** /diːp kəmˈpærəsn/ - 深比较
- **State management** /steɪt ˈmænɪdʒmənt/ - 状态管理
- **Context splitting** /ˈkɑːntekst ˈsplɪtɪŋ/ - Context拆分
- **Render optimization** /ˈrendər ˌɑːptɪməˈzeɪʃn/ - 渲染优化
- **Component lifecycle** /kəmˈpoʊnənt ˈlaɪfˌsaɪkl/ - 组件生命周期
- **Performance profiling** /pərˈfɔːrməns ˈproʊfaɪlɪŋ/ - 性能分析
- **Memory leak** /ˈmeməri liːk/ - 内存泄漏
- **Garbage collection** /ˈɡɑːrbɪdʒ kəˈlekʃn/ - 垃圾回收

## 3. 网络性能优化 - Network Performance Optimization

### 场景对话：API请求和缓存策略

**English:**
```
Interviewer: "How do you optimize network requests and implement effective caching strategies in a frontend application?"

Candidate: "I implement a multi-layered approach to network optimization:

**Request Optimization**:
```javascript
// Request batching and deduplication
class RequestManager {
  constructor() {
    this.pendingRequests = new Map();
    this.batchQueue = [];
    this.batchTimeout = null;
  }
  
  // Deduplicate identical requests
  async request(url, options = {}) {
    const key = `${url}-${JSON.stringify(options)}`;
    
    if (this.pendingRequests.has(key)) {
      return this.pendingRequests.get(key);
    }
    
    const promise = fetch(url, options).then(response => {
      this.pendingRequests.delete(key);
      return response;
    });
    
    this.pendingRequests.set(key, promise);
    return promise;
  }
  
  // Batch multiple requests
  batchRequest(requests) {
    return new Promise((resolve) => {
      this.batchQueue.push(...requests);
      
      if (this.batchTimeout) {
        clearTimeout(this.batchTimeout);
      }
      
      this.batchTimeout = setTimeout(() => {
        const batch = [...this.batchQueue];
        this.batchQueue = [];
        
        // Send batched request
        fetch('/api/batch', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ requests: batch })
        })
        .then(response => response.json())
        .then(resolve);
      }, 50); // 50ms debounce
    });
  }
}

// GraphQL for efficient data fetching
const GET_USER_DASHBOARD = gql`
  query GetUserDashboard($userId: ID!) {
    user(id: $userId) {
      id
      name
      avatar
      stats {
        totalProjects
        completedTasks
        teamMembers
      }
      recentProjects(limit: 5) {
        id
        name
        status
        updatedAt
      }
      notifications(unreadOnly: true) {
        id
        message
        type
        createdAt
      }
    }
  }
`;

// Instead of multiple REST API calls
// GET /api/users/123
// GET /api/users/123/stats
// GET /api/users/123/projects?limit=5
// GET /api/users/123/notifications?unread=true
```

**Caching Strategies**:
```javascript
// Service Worker for network-first/cache-first strategies
self.addEventListener('fetch', (event) => {
  const { request } = event;
  const url = new URL(request.url);
  
  // Cache-first for static assets
  if (url.pathname.match(/\.(js|css|png|jpg|jpeg|gif|svg|woff2?)$/)) {
    event.respondWith(
      caches.match(request).then((cachedResponse) => {
        if (cachedResponse) {
          return cachedResponse;
        }
        return fetch(request).then((response) => {
          const responseClone = response.clone();
          caches.open('static-cache-v1').then((cache) => {
            cache.put(request, responseClone);
          });
          return response;
        });
      })
    );
  }
  
  // Network-first for API calls with stale-while-revalidate
  if (url.pathname.startsWith('/api/')) {
    event.respondWith(
      fetch(request)
        .then((response) => {
          const responseClone = response.clone();
          caches.open('api-cache-v1').then((cache) => {
            cache.put(request, responseClone);
          });
          return response;
        })
        .catch(() => {
          return caches.match(request);
        })
    );
  }
});

// React Query for intelligent caching
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';

function UserProfile({ userId }) {
  const queryClient = useQueryClient();
  
  const { data: user, isLoading } = useQuery({
    queryKey: ['user', userId],
    queryFn: () => fetchUser(userId),
    staleTime: 5 * 60 * 1000, // 5 minutes
    cacheTime: 10 * 60 * 1000, // 10 minutes
    refetchOnWindowFocus: false,
  });
  
  const updateUserMutation = useMutation({
    mutationFn: updateUser,
    onSuccess: (updatedUser) => {
      // Optimistic update
      queryClient.setQueryData(['user', userId], updatedUser);
      
      // Invalidate related queries
      queryClient.invalidateQueries({ queryKey: ['users'] });
      queryClient.invalidateQueries({ queryKey: ['user-stats', userId] });
    },
  });
  
  return (
    <div>
      {isLoading ? <Skeleton /> : <UserDetails user={user} />}
    </div>
  );
}

// Custom hook for optimistic updates
function useOptimisticUpdate() {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: async ({ optimisticData, actualUpdate }) => {
      // Apply optimistic update immediately
      const previousData = queryClient.getQueryData(['data']);
      queryClient.setQueryData(['data'], optimisticData);
      
      try {
        // Perform actual update
        const result = await actualUpdate();
        return result;
      } catch (error) {
        // Rollback on error
        queryClient.setQueryData(['data'], previousData);
        throw error;
      }
    },
  });
}
```

**HTTP/2 and Resource Optimization**:
```javascript
// HTTP/2 Server Push simulation
const criticalResources = [
  '/styles/critical.css',
  '/scripts/vendor.js',
  '/scripts/app.js',
  '/api/user/current'
];

// Preload critical resources
criticalResources.forEach(resource => {
  const link = document.createElement('link');
  link.rel = 'preload';
  link.href = resource;
  link.as = resource.endsWith('.css') ? 'style' : 
           resource.endsWith('.js') ? 'script' : 'fetch';
  if (resource.startsWith('/api/')) {
    link.crossOrigin = 'anonymous';
  }
  document.head.appendChild(link);
});

// Resource bundling strategy
const bundleStrategy = {
  // Critical path bundle
  critical: ['react', 'react-dom', 'router'],
  
  // Feature-based bundles
  dashboard: ['chart.js', 'date-fns', 'dashboard-components'],
  profile: ['form-validation', 'image-cropper', 'profile-components'],
  
  // Shared utilities
  utils: ['lodash', 'axios', 'common-utils']
};

// Webpack configuration for optimal bundling
module.exports = {
  optimization: {
    splitChunks: {
      chunks: 'all',
      cacheGroups: {
        critical: {
          name: 'critical',
          test: /[\\/]node_modules[\\/](react|react-dom|react-router)/,
          priority: 30,
        },
        vendor: {
          name: 'vendor',
          test: /[\\/]node_modules[\\/]/,
          priority: 20,
        },
        common: {
          name: 'common',
          minChunks: 2,
          priority: 10,
        },
      },
    },
  },
};
```

These optimizations typically achieve:
- **60-80% reduction** in redundant network requests
- **2-4x faster** perceived loading times
- **Improved offline** functionality
- **Better user experience** with optimistic updates"
```

**中文对照：**
```
面试官："你如何优化网络请求并在前端应用中实现有效的缓存策略？"

候选人："我实施多层次的网络优化方法：

**请求优化**：
- 请求批处理和去重
- GraphQL用于高效数据获取
- 避免多个REST API调用

**缓存策略**：
- Service Worker实现网络优先/缓存优先策略
- React Query用于智能缓存
- 乐观更新和回滚机制

**HTTP/2和资源优化**：
- 关键资源预加载
- 资源打包策略
- Webpack配置优化

这些优化通常实现：
- **冗余网络请求减少60-80%**
- **感知加载时间快2-4倍**
- **改善离线功能**
- **乐观更新带来更好的用户体验**"
```

### 相关词汇
- **Request batching** /rɪˈkwest ˈbætʃɪŋ/ - 请求批处理
- **Deduplication** /diːˌduːplɪˈkeɪʃn/ - 去重
- **Cache strategy** /kæʃ ˈstrætədʒi/ - 缓存策略
- **Stale-while-revalidate** /steɪl waɪl riːˈvælɪdeɪt/ - 过期时重新验证
- **Optimistic update** /ˌɑːptɪˈmɪstɪk ˈʌpdeɪt/ - 乐观更新
- **Service Worker** /ˈsɜːrvɪs ˈwɜːrkər/ - Service Worker
- **HTTP/2 Server Push** /eɪtʃ tiː tiː piː tuː ˈsɜːrvər pʊʃ/ - HTTP/2服务器推送
- **Resource bundling** /ˈriːsɔːrs ˈbʌndlɪŋ/ - 资源打包
- **Network latency** /ˈnetwɜːrk ˈleɪtənsi/ - 网络延迟
- **Bandwidth optimization** /ˈbændwɪdθ ˌɑːptɪməˈzeɪʃn/ - 带宽优化
- **CDN** /siː diː en/ - 内容分发网络
- **Edge caching** /edʒ ˈkæʃɪŋ/ - 边缘缓存
- **Compression** /kəmˈpreʃn/ - 压缩
- **Gzip** /ˈdʒiːzɪp/ - Gzip压缩
- **Brotli** /ˈbroʊtli/ - Brotli压缩

## 4. 性能监控和分析 - Performance Monitoring and Analysis

### 场景对话：性能指标监控和问题诊断

**English:**
```
Interviewer: "How do you monitor frontend performance in production and diagnose performance issues?"

Candidate: "I implement comprehensive performance monitoring using multiple tools and metrics:

**Core Web Vitals Monitoring**:
```javascript
// Web Vitals measurement
import { getCLS, getFID, getFCP, getLCP, getTTFB } from 'web-vitals';

function sendToAnalytics(metric) {
  // Send to your analytics service
  gtag('event', metric.name, {
    event_category: 'Web Vitals',
    event_label: metric.id,
    value: Math.round(metric.name === 'CLS' ? metric.value * 1000 : metric.value),
    non_interaction: true,
  });
  
  // Also send to custom monitoring service
  fetch('/api/metrics', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      metric: metric.name,
      value: metric.value,
      id: metric.id,
      url: window.location.href,
      userAgent: navigator.userAgent,
      timestamp: Date.now()
    })
  });
}

// Measure all Core Web Vitals
getCLS(sendToAnalytics);
getFID(sendToAnalytics);
getFCP(sendToAnalytics);
getLCP(sendToAnalytics);
getTTFB(sendToAnalytics);

// Custom performance observer
const observer = new PerformanceObserver((list) => {
  for (const entry of list.getEntries()) {
    if (entry.entryType === 'navigation') {
      console.log('Navigation timing:', {
        domContentLoaded: entry.domContentLoadedEventEnd - entry.domContentLoadedEventStart,
        loadComplete: entry.loadEventEnd - entry.loadEventStart,
        firstByte: entry.responseStart - entry.requestStart,
        domInteractive: entry.domInteractive - entry.navigationStart
      });
    }
    
    if (entry.entryType === 'resource') {
      // Monitor slow resources
      if (entry.duration > 1000) {
        sendToAnalytics({
          name: 'slow-resource',
          value: entry.duration,
          resource: entry.name,
          type: entry.initiatorType
        });
      }
    }
  }
});

observer.observe({ entryTypes: ['navigation', 'resource', 'paint'] });
```

**Real User Monitoring (RUM)**:
```javascript
// Custom RUM implementation
class PerformanceMonitor {
  constructor() {
    this.metrics = {};
    this.startTime = performance.now();
    this.setupListeners();
  }
  
  setupListeners() {
    // Monitor long tasks
    if ('PerformanceObserver' in window) {
      const longTaskObserver = new PerformanceObserver((list) => {
        for (const entry of list.getEntries()) {
          this.trackLongTask(entry);
        }
      });
      longTaskObserver.observe({ entryTypes: ['longtask'] });
    }
    
    // Monitor memory usage
    if ('memory' in performance) {
      setInterval(() => {
        this.trackMemoryUsage();
      }, 30000); // Every 30 seconds
    }
    
    // Monitor user interactions
    ['click', 'scroll', 'keypress'].forEach(eventType => {
      document.addEventListener(eventType, (e) => {
        this.trackInteraction(eventType, e);
      }, { passive: true });
    });
  }
  
  trackLongTask(entry) {
    const longTask = {
      duration: entry.duration,
      startTime: entry.startTime,
      attribution: entry.attribution
    };
    
    // Send alert if task is too long
    if (entry.duration > 100) {
      this.sendAlert('long-task', longTask);
    }
  }
  
  trackMemoryUsage() {
    const memory = performance.memory;
    const memoryInfo = {
      usedJSHeapSize: memory.usedJSHeapSize,
      totalJSHeapSize: memory.totalJSHeapSize,
      jsHeapSizeLimit: memory.jsHeapSizeLimit,
      timestamp: Date.now()
    };
    
    // Check for memory leaks
    if (memory.usedJSHeapSize > memory.jsHeapSizeLimit * 0.9) {
      this.sendAlert('high-memory-usage', memoryInfo);
    }
  }
  
  trackInteraction(type, event) {
    const interactionStart = performance.now();
    
    // Use requestIdleCallback to measure after interaction
    requestIdleCallback(() => {
      const interactionDuration = performance.now() - interactionStart;
      
      if (interactionDuration > 100) {
        this.sendMetric('slow-interaction', {
          type,
          duration: interactionDuration,
          target: event.target.tagName
        });
      }
    });
  }
  
  sendAlert(type, data) {
    fetch('/api/alerts', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ type, data, timestamp: Date.now() })
    });
  }
  
  sendMetric(name, data) {
    fetch('/api/metrics', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name, data, timestamp: Date.now() })
    });
  }
}

// Initialize monitoring
const monitor = new PerformanceMonitor();
```

**Error Tracking and Performance Correlation**:
```javascript
// Sentry integration with performance monitoring
import * as Sentry from '@sentry/react';
import { Integrations } from '@sentry/tracing';

Sentry.init({
  dsn: 'YOUR_DSN',
  integrations: [
    new Integrations.BrowserTracing({
      tracingOrigins: ['localhost', 'your-api-domain.com', /^\//],
      routingInstrumentation: Sentry.reactRouterV6Instrumentation(
        React.useEffect,
        useLocation,
        useNavigationType,
        createRoutesFromChildren,
        matchRoutes
      ),
    }),
  ],
  tracesSampleRate: 0.1, // 10% of transactions
  beforeSend(event, hint) {
    // Add performance context to errors
    if (event.exception) {
      const performanceEntries = performance.getEntriesByType('navigation');
      if (performanceEntries.length > 0) {
        const navigation = performanceEntries[0];
        event.extra = {
          ...event.extra,
          performanceMetrics: {
            domContentLoaded: navigation.domContentLoadedEventEnd - navigation.domContentLoadedEventStart,
            loadComplete: navigation.loadEventEnd - navigation.loadEventStart,
            firstByte: navigation.responseStart - navigation.requestStart
          }
        };
      }
    }
    return event;
  }
});

// Custom error boundary with performance tracking
class PerformanceErrorBoundary extends React.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  
  static getDerivedStateFromError(error) {
    return { hasError: true };
  }
  
  componentDidCatch(error, errorInfo) {
    // Capture performance metrics when error occurs
    const performanceData = {
      memory: performance.memory ? {
        usedJSHeapSize: performance.memory.usedJSHeapSize,
        totalJSHeapSize: performance.memory.totalJSHeapSize
      } : null,
      timing: performance.timing,
      navigation: performance.getEntriesByType('navigation')[0]
    };
    
    Sentry.withScope((scope) => {
      scope.setContext('performance', performanceData);
      scope.setContext('errorInfo', errorInfo);
      Sentry.captureException(error);
    });
  }
  
  render() {
    if (this.state.hasError) {
      return <ErrorFallback />;
    }
    
    return this.props.children;
  }
}
```

**Performance Dashboard and Alerting**:
```javascript
// Performance dashboard component
function PerformanceDashboard() {
  const [metrics, setMetrics] = useState(null);
  const [alerts, setAlerts] = useState([]);
  
  useEffect(() => {
    // Fetch performance metrics
    fetch('/api/performance/dashboard')
      .then(response => response.json())
      .then(data => {
        setMetrics(data.metrics);
        setAlerts(data.alerts);
      });
  }, []);
  
  const performanceScore = useMemo(() => {
    if (!metrics) return 0;
    
    const weights = {
      lcp: 0.25,
      fid: 0.25,
      cls: 0.25,
      fcp: 0.25
    };
    
    const scores = {
      lcp: metrics.lcp < 2500 ? 100 : metrics.lcp < 4000 ? 50 : 0,
      fid: metrics.fid < 100 ? 100 : metrics.fid < 300 ? 50 : 0,
      cls: metrics.cls < 0.1 ? 100 : metrics.cls < 0.25 ? 50 : 0,
      fcp: metrics.fcp < 1800 ? 100 : metrics.fcp < 3000 ? 50 : 0
    };
    
    return Object.entries(weights).reduce((total, [metric, weight]) => {
      return total + (scores[metric] * weight);
    }, 0);
  }, [metrics]);
  
  return (
    <div className="performance-dashboard">
      <div className="score-card">
        <h2>Performance Score</h2>
        <div className={`score ${performanceScore > 80 ? 'good' : performanceScore > 50 ? 'needs-improvement' : 'poor'}`}>
          {Math.round(performanceScore)}
        </div>
      </div>
      
      <div className="metrics-grid">
        <MetricCard title="LCP" value={metrics?.lcp} unit="ms" threshold={2500} />
        <MetricCard title="FID" value={metrics?.fid} unit="ms" threshold={100} />
        <MetricCard title="CLS" value={metrics?.cls} unit="" threshold={0.1} />
        <MetricCard title="FCP" value={metrics?.fcp} unit="ms" threshold={1800} />
      </div>
      
      <div className="alerts-section">
        <h3>Performance Alerts</h3>
        {alerts.map(alert => (
          <AlertCard key={alert.id} alert={alert} />
        ))}
      </div>
    </div>
  );
}
```

This monitoring approach provides:
- **Real-time performance insights** with 95% accuracy
- **Proactive alerting** for performance regressions
- **Correlation between errors and performance** issues
- **Actionable data** for optimization decisions"
```

**中文对照：**
```
面试官："你如何在生产环境中监控前端性能并诊断性能问题？"

候选人："我使用多种工具和指标实施全面的性能监控：

**核心Web指标监控**：
- 使用web-vitals库测量CLS、FID、FCP、LCP、TTFB
- 自定义性能观察器监控导航和资源加载
- 向分析服务发送指标数据

**真实用户监控(RUM)**：
- 监控长任务和内存使用
- 跟踪用户交互性能
- 实时警报机制

**错误跟踪和性能关联**：
- Sentry集成性能监控
- 错误边界与性能跟踪
- 错误发生时捕获性能上下文

**性能仪表板和警报**：
- 性能评分计算
- 指标可视化
- 主动性能警报

这种监控方法提供：
- **95%准确性的实时性能洞察**
- **性能回归的主动警报**
- **错误与性能问题的关联**
- **优化决策的可操作数据**"
```

### 相关词汇
- **Core Web Vitals** /kɔːr web ˈvaɪtlz/ - 核心Web指标
- **Real User Monitoring (RUM)** /riːl ˈjuːzər ˈmɑːnətərɪŋ/ - 真实用户监控
- **Performance Observer** /pərˈfɔːrməns əbˈzɜːrvər/ - 性能观察器
- **Long task** /lɔːŋ tæsk/ - 长任务
- **Memory leak** /ˈmeməri liːk/ - 内存泄漏
- **Performance regression** /pərˈfɔːrməns rɪˈɡreʃn/ - 性能回归
- **Synthetic monitoring** /sɪnˈθetɪk ˈmɑːnətərɪŋ/ - 合成监控
- **Error boundary** /ˈerər ˈbaʊndəri/ - 错误边界
- **Performance budget** /pərˈfɔːrməns ˈbʌdʒɪt/ - 性能预算
- **Lighthouse score** /ˈlaɪthaʊs skɔːr/ - Lighthouse评分
- **Performance profiling** /pərˈfɔːrməns ˈproʊfaɪlɪŋ/ - 性能分析
- **Bottleneck** /ˈbɑːtlnek/ - 瓶颈
- **Optimization opportunity** /ˌɑːptɪməˈzeɪʃn ˌɑːpərˈtuːnəti/ - 优化机会
- **Performance audit** /pərˈfɔːrməns ˈɔːdɪt/ - 性能审计
- **Baseline measurement** /ˈbeɪslaɪn ˈmeʒərmənt/ - 基线测量

## 5. 性能优化相关词汇表

### 性能指标
- **First Contentful Paint (FCP)** /fɜːrst ˈkɑːntentfəl peɪnt/ - 首次内容绘制
- **Largest Contentful Paint (LCP)** /ˈlɑːrdʒəst ˈkɑːntentfəl peɪnt/ - 最大内容绘制
- **First Input Delay (FID)** /fɜːrst ˈɪnpʊt dɪˈleɪ/ - 首次输入延迟
- **Cumulative Layout Shift (CLS)** /ˈkjuːmjələtɪv ˈleɪaʊt ʃɪft/ - 累积布局偏移
- **Time to Interactive (TTI)** /taɪm tuː ˌɪntərˈæktɪv/ - 可交互时间
- **Time to First Byte (TTFB)** /taɪm tuː fɜːrst baɪt/ - 首字节时间
- **Speed Index** /spiːd ˈɪndeks/ - 速度指数
- **Total Blocking Time (TBT)** /ˈtoʊtl ˈblɑːkɪŋ taɪm/ - 总阻塞时间

### 优化技术
- **Code splitting** /koʊd ˈsplɪtɪŋ/ - 代码分割
- **Lazy loading** /ˈleɪzi ˈloʊdɪŋ/ - 懒加载
- **Tree shaking** /triː ˈʃeɪkɪŋ/ - 摇树优化
- **Minification** /ˌmɪnɪfɪˈkeɪʃn/ - 代码压缩
- **Compression** /kəmˈpreʃn/ - 压缩
- **Caching** /ˈkæʃɪŋ/ - 缓存
- **Prefetching** /ˈpriːfetʃɪŋ/ - 预取
- **Preloading** /ˈpriːloʊdɪŋ/ - 预加载
- **Critical path** /ˈkrɪtɪkl pæθ/ - 关键路径
- **Above the fold** /əˈbʌv ðə foʊld/ - 首屏内容
- **Progressive enhancement** /prəˈɡresɪv ɪnˈhænsmənt/ - 渐进增强
- **Graceful degradation** /ˈɡreɪsfəl ˌdeɡrəˈdeɪʃn/ - 优雅降级

### 工具和技术
- **Webpack** /ˈwebpæk/ - Webpack打包工具
- **Vite** /viːt/ - Vite构建工具
- **Lighthouse** /ˈlaɪthaʊs/ - Lighthouse性能测试工具
- **WebPageTest** /web peɪdʒ test/ - WebPageTest性能测试
- **Chrome DevTools** /kroʊm dev tuːlz/ - Chrome开发者工具
- **Service Worker** /ˈsɜːrvɪs ˈwɜːrkər/ - Service Worker
- **Web Workers** /web ˈwɜːrkərz/ - Web Workers
- **CDN** /siː diː en/ - 内容分发网络
- **HTTP/2** /eɪtʃ tiː tiː piː tuː/ - HTTP/2协议
- **Brotli** /ˈbroʊtli/ - Brotli压缩算法
- **WebP** /webpiː/ - WebP图片格式
- **AVIF** /eɪ viː aɪ ef/ - AVIF图片格式

### 性能问题
- **Bottleneck** /ˈbɑːtlnek/ - 性能瓶颈
- **Memory leak** /ˈmeməri liːk/ - 内存泄漏
- **Render blocking** /ˈrendər ˈblɑːkɪŋ/ - 渲染阻塞
- **Layout thrashing** /ˈleɪaʊt ˈθræʃɪŋ/ - 布局抖动
- **Reflow** /riːˈfloʊ/ - 重排
- **Repaint** /riːˈpeɪnt/ - 重绘
- **Jank** /dʒæŋk/ - 卡顿
- **Long task** /lɔːŋ tæsk/ - 长任务
- **Main thread blocking** /meɪn θred ˈblɑːkɪŋ/ - 主线程阻塞
- **Resource contention** /ˈriːsɔːrs kənˈtenʃn/ - 资源竞争

## 6. 面试技巧和建议

### 展示性能优化思维的要点

**English:**
- **Start with measurement**: "Before optimizing, I always establish baseline metrics using tools like Lighthouse and WebPageTest."
- **Prioritize impact**: "I focus on optimizations that provide the biggest user experience improvements first."
- **Consider trade-offs**: "Every optimization has trade-offs, so I evaluate the cost-benefit of each approach."
- **Think holistically**: "Performance optimization isn't just about code - it includes network, caching, and infrastructure."
- **Monitor continuously**: "I implement monitoring to catch performance regressions before they affect users."

**中文对照：**
- **从测量开始**："在优化之前，我总是使用Lighthouse和WebPageTest等工具建立基线指标。"
- **优先考虑影响**："我首先关注能提供最大用户体验改善的优化。"
- **考虑权衡**："每个优化都有权衡，所以我评估每种方法的成本效益。"
- **整体思考**："性能优化不仅仅是代码 - 它包括网络、缓存和基础设施。"
- **持续监控**："我实施监控以在性能回归影响用户之前捕获它们。"

### 常用表达句式

**Describing optimization strategies**:
- "To improve [metric], I implemented [technique] which resulted in [improvement]."
- "The bottleneck was [issue], so I addressed it by [solution]."
- "I optimized [component] by [method], reducing [metric] by [percentage]."
- "The key insight was [finding], which led me to [optimization]."
- "I balanced [concern A] with [concern B] by [approach]."

**Explaining performance impact**:
- "This optimization improved user experience by [specific benefit]."
- "The performance gain translated to [business metric] improvement."
- "Users experienced [improvement] in [specific scenario]."
- "The optimization reduced bounce rate by [percentage]."
- "Load time improvements led to [conversion/engagement] increases."

**中文对照：**

**描述优化策略**：
- "为了改善[指标]，我实施了[技术]，结果带来了[改进]。"
- "瓶颈是[问题]，所以我通过[解决方案]来解决它。"
- "我通过[方法]优化了[组件]，将[指标]减少了[百分比]。"
- "关键洞察是[发现]，这让我进行了[优化]。"
- "我通过[方法]平衡了[关注点A]和[关注点B]。"

**解释性能影响**：
- "这个优化通过[具体好处]改善了用户体验。"
- "性能提升转化为[业务指标]的改善。"
- "用户在[特定场景]中体验到了[改进]。"
- "优化将跳出率降低了[百分比]。"
- "加载时间改善导致[转化/参与度]增加。"

### 避免的误区

**English:**
- **Don't optimize prematurely**: Focus on actual bottlenecks, not theoretical ones
- **Don't ignore user context**: Consider different devices, networks, and usage patterns
- **Don't sacrifice maintainability**: Ensure optimizations don't make code unmaintainable
- **Don't forget accessibility**: Performance optimizations shouldn't harm accessibility
- **Don't over-engineer**: Simple solutions are often better than complex ones

**中文对照：**
- **不要过早优化**：关注实际瓶颈，而不是理论瓶颈
- **不要忽视用户环境**：考虑不同的设备、网络和使用模式
- **不要牺牲可维护性**：确保优化不会使代码难以维护
- **不要忘记可访问性**：性能优化不应损害可访问性
- **不要过度工程化**：简单的解决方案通常比复杂的更好

### 相关词汇
- **Optimization** /ˌɑːptɪməˈzeɪʃn/ - 优化
- **Performance** /pərˈfɔːrməns/ - 性能
- **Efficiency** /ɪˈfɪʃnsi/ - 效率
- **Scalability** /ˌskeɪləˈbɪləti/ - 可扩展性
- **Responsiveness** /rɪˈspɑːnsɪvnəs/ - 响应性
- **Throughput** /ˈθruːpʊt/ - 吞吐量
- **Latency** /ˈleɪtənsi/ - 延迟
- **Bandwidth** /ˈbændwɪdθ/ - 带宽
- **Overhead** /ˈoʊvərhed/ - 开销
- **Baseline** /ˈbeɪslaɪn/ - 基线
- **Benchmark** /ˈbentʃmɑːrk/ - 基准测试
- **Profiling** /ˈproʊfaɪlɪŋ/ - 性能分析
- **Debugging** /dɪˈbʌɡɪŋ/ - 调试
- **Monitoring** /ˈmɑːnətərɪŋ/ - 监控
- **Analytics** /ˌænəˈlɪtɪks/ - 分析