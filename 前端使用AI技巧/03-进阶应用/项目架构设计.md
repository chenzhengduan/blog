# AI辅助项目架构设计

## 🏗️ 架构设计的重要性

### 1. 为什么需要好的架构
- **可维护性**: 清晰的架构让代码更易维护和扩展
- **可测试性**: 良好的分层设计便于单元测试和集成测试
- **团队协作**: 统一的架构规范提高团队开发效率
- **性能优化**: 合理的架构设计有助于性能优化

### 2. AI在架构设计中的价值
- **快速原型**: 快速生成项目脚手架和基础架构
- **最佳实践**: 基于行业最佳实践的架构建议
- **技术选型**: 根据项目需求推荐合适的技术栈
- **代码组织**: 提供清晰的目录结构和文件组织方案

## 🎯 架构设计提示词策略

### 1. 项目初始化架构

```markdown
# 项目架构设计提示词
请为我设计一个现代化的前端项目架构，项目信息如下：

**项目类型**: 企业级管理后台
**技术栈**: Vue 3 + TypeScript + Vite
**主要功能**: 用户管理、权限控制、数据可视化、文件上传
**团队规模**: 5-8人开发团队
**预期规模**: 中大型项目（50+页面，100+组件）

**设计要求**:
1. 清晰的目录结构
2. 模块化设计
3. 可扩展的组件体系
4. 统一的状态管理
5. 完善的类型定义
6. 开发工具配置

请提供：
- 完整的目录结构
- 核心文件的基础代码
- 配置文件设置
- 开发规范建议
```

**AI生成的项目架构：**

```
# 企业级管理后台架构设计

## 📁 目录结构

```
enterprise-admin/
├── public/                          # 静态资源
│   ├── icons/                      # 图标文件
│   ├── images/                     # 图片资源
│   └── favicon.ico
├── src/
│   ├── api/                        # API接口层
│   │   ├── modules/               # 按模块分组的API
│   │   │   ├── auth.ts
│   │   │   ├── user.ts
│   │   │   └── dashboard.ts
│   │   ├── types/                 # API类型定义
│   │   ├── request.ts             # 请求封装
│   │   └── index.ts
│   ├── assets/                     # 资源文件
│   │   ├── styles/                # 样式文件
│   │   │   ├── globals.css
│   │   │   ├── variables.css
│   │   │   └── components.css
│   │   ├── fonts/                 # 字体文件
│   │   └── images/                # 图片资源
│   ├── components/                 # 组件库
│   │   ├── ui/                    # 基础UI组件
│   │   │   ├── Button/
│   │   │   │   ├── index.tsx
│   │   │   │   ├── Button.module.css
│   │   │   │   └── Button.test.tsx
│   │   │   ├── Input/
│   │   │   ├── Modal/
│   │   │   └── index.ts           # 统一导出
│   │   ├── business/              # 业务组件
│   │   │   ├── UserTable/
│   │   │   ├── PermissionTree/
│   │   │   └── DataChart/
│   │   ├── layout/                # 布局组件
│   │   │   ├── Header/
│   │   │   ├── Sidebar/
│   │   │   ├── Footer/
│   │   │   └── MainLayout/
│   │   └── common/                # 通用组件
│   │       ├── Loading/
│   │       ├── ErrorBoundary/
│   │       └── ProtectedRoute/
│   ├── hooks/                      # 自定义Hooks
│   │   ├── useAuth.ts
│   │   ├── usePermission.ts
│   │   ├── useLocalStorage.ts
│   │   └── index.ts
│   ├── pages/                      # 页面组件
│   │   ├── Dashboard/
│   │   │   ├── index.vue
│   │   │   ├── components/        # 页面专用组件
│   │   │   └── composables/       # 页面专用组合式函数
│   │   ├── User/
│   │   │   ├── List/
│   │   │   ├── Detail/
│   │   │   └── Edit/
│   │   ├── Permission/
│   │   ├── Login/
│   │   └── NotFound/
│   ├── router/                     # 路由配置
│   │   ├── index.ts
│   │   ├── routes.ts
│   │   └── guards.ts
│   ├── store/                      # 状态管理
│   │   ├── modules/               # 按模块分组
│   │   │   ├── auth.ts
│   │   │   ├── user.ts
│   │   │   └── app.ts
│   │   ├── types.ts               # Store类型定义
│   │   └── index.ts
│   ├── utils/                      # 工具函数
│   │   ├── format.ts              # 格式化工具
│   │   ├── validation.ts          # 验证工具
│   │   ├── storage.ts             # 存储工具
│   │   ├── permission.ts          # 权限工具
│   │   └── index.ts
│   ├── types/                      # 全局类型定义
│   │   ├── api.ts                 # API类型
│   │   ├── user.ts                # 用户类型
│   │   ├── common.ts              # 通用类型
│   │   └── index.ts
│   ├── constants/                  # 常量定义
│   │   ├── api.ts                 # API常量
│   │   ├── routes.ts              # 路由常量
│   │   ├── permissions.ts         # 权限常量
│   │   └── index.ts
│   ├── config/                     # 配置文件
│   │   ├── env.ts                 # 环境配置
│   │   ├── theme.ts               # 主题配置
│   │   └── index.ts
│   ├── App.vue                     # 根组件
│   ├── main.ts                     # 入口文件
│   └── vite-env.d.ts              # Vite类型声明
├── tests/                          # 测试文件
│   ├── __mocks__/                 # Mock文件
│   ├── setup.ts                   # 测试设置
│   └── utils.ts                   # 测试工具
├── docs/                           # 项目文档
│   ├── api.md                     # API文档
│   ├── components.md              # 组件文档
│   └── deployment.md              # 部署文档
├── .env.example                    # 环境变量示例
├── .env.development               # 开发环境变量
├── .env.production                # 生产环境变量
├── .gitignore
├── .eslintrc.js                   # ESLint配置
├── .prettierrc                    # Prettier配置
├── tsconfig.json                  # TypeScript配置
├── vite.config.ts                 # Vite配置
├── tailwind.config.js             # Tailwind配置
├── package.json
└── README.md
```

## 🔧 核心文件实现

### 1. 请求封装 (src/api/request.ts)

```typescript
import axios, { AxiosInstance, AxiosRequestConfig, AxiosResponse } from 'axios'
import { ElMessage } from 'element-plus'
import { getToken, removeToken } from '@/utils/storage'
import { API_BASE_URL } from '@/constants/api'

// 请求配置接口
interface RequestConfig extends AxiosRequestConfig {
  skipErrorHandler?: boolean
  skipAuth?: boolean
}

// 响应数据接口
interface ApiResponse<T = any> {
  code: number
  data: T
  message: string
  success: boolean
}

class RequestService {
  private instance: AxiosInstance

  constructor() {
    this.instance = axios.create({
      baseURL: API_BASE_URL,
      timeout: 10000,
      headers: {
        'Content-Type': 'application/json'
      }
    })

    this.setupInterceptors()
  }

  private setupInterceptors() {
    // 请求拦截器
    this.instance.interceptors.request.use(
      (config: RequestConfig) => {
        // 添加认证token
        if (!config.skipAuth) {
          const token = getToken()
          if (token) {
            config.headers = {
              ...config.headers,
              Authorization: `Bearer ${token}`
            }
          }
        }

        // 添加请求时间戳
        config.metadata = { startTime: new Date() }
        
        return config
      },
      (error) => {
        return Promise.reject(error)
      }
    )

    // 响应拦截器
    this.instance.interceptors.response.use(
      (response: AxiosResponse<ApiResponse>) => {
        const { data } = response
        
        // 记录请求耗时
        const endTime = new Date()
        const duration = endTime.getTime() - response.config.metadata?.startTime?.getTime()
        console.log(`API ${response.config.url} took ${duration}ms`)

        // 处理业务错误
        if (!data.success) {
          if (!response.config.skipErrorHandler) {
            ElMessage.error(data.message || '请求失败')
          }
          return Promise.reject(new Error(data.message))
        }

        return data.data
      },
      (error) => {
        // 处理HTTP错误
        if (error.response) {
          const { status, data } = error.response
          
          switch (status) {
            case 401:
              ElMessage.error('登录已过期，请重新登录')
              removeToken()
              window.location.href = '/login'
              break
            case 403:
              ElMessage.error('没有权限访问该资源')
              break
            case 404:
              ElMessage.error('请求的资源不存在')
              break
            case 500:
              ElMessage.error('服务器内部错误')
              break
            default:
              ElMessage.error(data?.message || '网络错误')
          }
        } else if (error.request) {
          ElMessage.error('网络连接失败，请检查网络')
        } else {
          ElMessage.error('请求配置错误')
        }

        return Promise.reject(error)
      }
    )
  }

  // GET请求
  get<T = any>(url: string, config?: RequestConfig): Promise<T> {
    return this.instance.get(url, config)
  }

  // POST请求
  post<T = any>(url: string, data?: any, config?: RequestConfig): Promise<T> {
    return this.instance.post(url, data, config)
  }

  // PUT请求
  put<T = any>(url: string, data?: any, config?: RequestConfig): Promise<T> {
    return this.instance.put(url, data, config)
  }

  // DELETE请求
  delete<T = any>(url: string, config?: RequestConfig): Promise<T> {
    return this.instance.delete(url, config)
  }

  // 文件上传
  upload<T = any>(url: string, file: File, onProgress?: (progress: number) => void): Promise<T> {
    const formData = new FormData()
    formData.append('file', file)

    return this.instance.post(url, formData, {
      headers: {
        'Content-Type': 'multipart/form-data'
      },
      onUploadProgress: (progressEvent) => {
        if (onProgress && progressEvent.total) {
          const progress = Math.round((progressEvent.loaded * 100) / progressEvent.total)
          onProgress(progress)
        }
      }
    })
  }
}

export const request = new RequestService()
export default request
```

### 2. 状态管理 (src/store/index.ts)

```typescript
import { createPinia } from 'pinia'
import { createPersistedState } from 'pinia-plugin-persistedstate'

// 创建Pinia实例
export const pinia = createPinia()

// 添加持久化插件
pinia.use(
  createPersistedState({
    storage: localStorage,
    key: 'enterprise-admin-store'
  })
)

export default pinia

// 导出各个store
export { useAuthStore } from './modules/auth'
export { useUserStore } from './modules/user'
export { useAppStore } from './modules/app'
```

### 3. 认证状态管理 (src/store/modules/auth.ts)

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import type { User } from '@/types/user'
import { login as loginApi, logout as logoutApi, getUserInfo } from '@/api/modules/auth'
import { setToken, removeToken } from '@/utils/storage'

export const useAuthStore = defineStore('auth', () => {
  // 状态
  const token = ref<string>('')
  const userInfo = ref<User | null>(null)
  const loading = ref(false)

  // 计算属性
  const isAuthenticated = computed(() => !!token.value && !!userInfo.value)

  // 登录
  const login = async (credentials: { username: string; password: string }) => {
    loading.value = true
    try {
      const response = await loginApi(credentials)
      token.value = response.token
      userInfo.value = response.user
      setToken(response.token)
      return response
    } finally {
      loading.value = false
    }
  }

  // 登出
  const logout = async () => {
    try {
      await logoutApi()
    } finally {
      token.value = ''
      userInfo.value = null
      removeToken()
    }
  }

  // 更新用户信息
  const updateUserInfo = async () => {
    if (!token.value) return
    
    try {
      const user = await getUserInfo()
      userInfo.value = user
    } catch (error) {
      console.error('Failed to update user info:', error)
    }
  }

  return {
    // 状态
    token,
    userInfo,
    loading,
    // 计算属性
    isAuthenticated,
    // 方法
    login,
    logout,
    updateUserInfo
  }
}, {
  persist: {
    paths: ['token', 'userInfo']
  }
})
```

### 4. 路由配置 (src/router/index.ts)

```typescript
import { createRouter, createWebHistory, type RouteRecordRaw } from 'vue-router'
import { ElLoading } from 'element-plus'
import MainLayout from '@/components/layout/MainLayout.vue'
import { ROUTES } from '@/constants/routes'
import { useAuthStore } from '@/store/modules/auth'

// 懒加载页面组件
const Dashboard = () => import('@/pages/Dashboard/index.vue')
const UserList = () => import('@/pages/User/List/index.vue')
const UserDetail = () => import('@/pages/User/Detail/index.vue')
const UserEdit = () => import('@/pages/User/Edit/index.vue')
const Permission = () => import('@/pages/Permission/index.vue')
const Login = () => import('@/pages/Login/index.vue')
const NotFound = () => import('@/pages/NotFound/index.vue')

// 路由配置
const routes: RouteRecordRaw[] = [
  {
    path: '/login',
    name: 'Login',
    component: Login,
    meta: {
      requiresAuth: false,
      title: '登录'
    }
  },
  {
    path: '/',
    component: MainLayout,
    meta: {
      requiresAuth: true
    },
    children: [
      {
        path: '',
        redirect: ROUTES.DASHBOARD
      },
      {
        path: ROUTES.DASHBOARD,
        name: 'Dashboard',
        component: Dashboard,
        meta: {
          title: '仪表盘',
          icon: 'dashboard'
        }
      },
      {
        path: ROUTES.USER.LIST,
        name: 'UserList',
        component: UserList,
        meta: {
          title: '用户列表',
          icon: 'user'
        }
      },
      {
        path: ROUTES.USER.DETAIL,
        name: 'UserDetail',
        component: UserDetail,
        meta: {
          title: '用户详情',
          hidden: true
        }
      },
      {
        path: ROUTES.USER.EDIT,
        name: 'UserEdit',
        component: UserEdit,
        meta: {
          title: '编辑用户',
          hidden: true
        }
      },
      {
        path: ROUTES.PERMISSION,
        name: 'Permission',
        component: Permission,
        meta: {
          title: '权限管理',
          icon: 'permission'
        }
      }
    ]
  },
  {
    path: '/:pathMatch(.*)*',
    name: 'NotFound',
    component: NotFound,
    meta: {
      title: '页面不存在'
    }
  }
]

// 创建路由实例
const router = createRouter({
  history: createWebHistory(),
  routes
})

// 全局前置守卫
router.beforeEach(async (to, from, next) => {
  // 显示加载状态
  const loading = ElLoading.service({
    lock: true,
    text: '页面加载中...',
    background: 'rgba(0, 0, 0, 0.7)'
  })

  try {
    const authStore = useAuthStore()
    
    // 检查是否需要认证
    if (to.meta.requiresAuth !== false) {
      if (!authStore.isAuthenticated) {
        next({ path: '/login', query: { redirect: to.fullPath } })
        return
      }
    }

    // 如果已登录访问登录页，重定向到首页
    if (to.path === '/login' && authStore.isAuthenticated) {
      next({ path: ROUTES.DASHBOARD })
      return
    }

    // 设置页面标题
    if (to.meta.title) {
      document.title = `${to.meta.title} - 企业管理后台`
    }

    next()
  } finally {
    loading.close()
  }
})

export default router
```

### 4. 类型定义 (src/types/index.ts)

```typescript
// 通用类型定义
export interface BaseEntity {
  id: string
  createdAt: string
  updatedAt: string
}

// 分页参数
export interface PaginationParams {
  page: number
  pageSize: number
  total?: number
}

// 分页响应
export interface PaginatedResponse<T> {
  data: T[]
  pagination: {
    current: number
    pageSize: number
    total: number
    totalPages: number
  }
}

// 排序参数
export interface SortParams {
  field: string
  order: 'asc' | 'desc'
}

// 筛选参数
export interface FilterParams {
  [key: string]: any
}

// 查询参数
export interface QueryParams extends PaginationParams {
  sort?: SortParams
  filters?: FilterParams
  search?: string
}

// API响应
export interface ApiResponse<T = any> {
  code: number
  data: T
  message: string
  success: boolean
}

// 表单状态
export interface FormState<T = any> {
  data: T
  loading: boolean
  errors: Record<string, string>
}

// 表格状态
export interface TableState<T = any> {
  data: T[]
  loading: boolean
  pagination: PaginationParams
  selectedRowKeys: string[]
  filters: FilterParams
  sorter: SortParams | null
}

// 权限相关
export interface Permission {
  id: string
  name: string
  code: string
  description?: string
  type: 'menu' | 'button' | 'api'
  parentId?: string
  children?: Permission[]
}

export interface Role {
  id: string
  name: string
  code: string
  description?: string
  permissions: Permission[]
}

// 用户相关
export interface User extends BaseEntity {
  username: string
  email: string
  phone?: string
  avatar?: string
  status: 'active' | 'inactive' | 'suspended'
  roles: Role[]
  profile?: UserProfile
}

export interface UserProfile {
  firstName: string
  lastName: string
  bio?: string
  location?: string
  website?: string
}

// 主题相关
export type ThemeMode = 'light' | 'dark' | 'auto'

export interface ThemeConfig {
  mode: ThemeMode
  primaryColor: string
  borderRadius: number
  fontSize: number
}

// 语言相关
export type Language = 'zh-CN' | 'en-US' | 'ja-JP'

export interface LanguageConfig {
  locale: Language
  messages: Record<string, string>
}

// 菜单相关
export interface MenuItem {
  key: string
  label: string
  icon?: string
  path?: string
  children?: MenuItem[]
  permission?: string
  hidden?: boolean
}

// 面包屑
export interface BreadcrumbItem {
  title: string
  path?: string
}

// 文件上传
export interface UploadFile {
  uid: string
  name: string
  status: 'uploading' | 'done' | 'error' | 'removed'
  url?: string
  thumbUrl?: string
  size?: number
  type?: string
  percent?: number
  response?: any
  error?: any
}

// 导出所有类型
export * from './api'
export * from './user'
export * from './common'
```

## 📋 开发规范

### 1. 命名规范

```typescript
// 文件命名：kebab-case
user-management.vue
api-service.ts

// 组件命名：PascalCase
const UserManagement = defineComponent({})

// 函数命名：camelCase
const fetchUserData = () => {}

// 常量命名：SCREAMING_SNAKE_CASE
const API_BASE_URL = 'https://api.example.com'

// 类型命名：PascalCase
interface UserProfile {}
type ThemeMode = 'light' | 'dark'
```

### 2. 目录组织原则

```markdown
# 目录组织原则

## 按功能模块分组
- 相关的文件放在同一个目录下
- 每个模块有独立的types、hooks、components

## 分层架构
- api: 数据访问层
- store: 状态管理层
- components: 视图层
- utils: 工具层

## 单一职责
- 每个文件只负责一个功能
- 组件职责单一，便于复用和测试

## 依赖方向
- 上层依赖下层，下层不依赖上层
- 业务组件可以依赖基础组件，反之不行
```

### 3. 代码组织模式

```vue
<!-- 组件文件结构模式 -->
<!-- components/ui/Button/index.vue -->

<template>
  <button
    :class="[
      'button',
      `button--${variant}`,
      `button--${size}`,
      className
    ]"
    @click="handleClick"
  >
    <slot />
  </button>
</template>

<script setup lang="ts">
import { computed } from 'vue'

// 1. 类型定义
interface ButtonProps {
  variant?: 'primary' | 'secondary'
  size?: 'small' | 'medium' | 'large'
  className?: string
}

// 2. Props定义
const props = withDefaults(defineProps<ButtonProps>(), {
  variant: 'primary',
  size: 'medium'
})

// 3. 事件定义
const emit = defineEmits<{
  click: []
}>()

// 4. 事件处理
const handleClick = () => {
  emit('click')
}
</script>

<style module>
.button {
  @apply px-4 py-2 rounded font-medium transition-colors;
}

.button--primary {
  @apply bg-blue-500 text-white hover:bg-blue-600;
}

.button--secondary {
  @apply bg-gray-200 text-gray-800 hover:bg-gray-300;
}

.button--small {
  @apply px-2 py-1 text-sm;
}

.button--medium {
  @apply px-4 py-2 text-base;
}

.button--large {
  @apply px-6 py-3 text-lg;
}
</style>
```

## 🚀 架构优化建议

### 1. 性能优化架构

```typescript
// 代码分割策略
// router/lazy-routes.ts

import { defineAsyncComponent } from 'vue'

// 按路由分割
export const Dashboard = defineAsyncComponent(() => import('@/pages/Dashboard/index.vue'))
export const UserManagement = defineAsyncComponent(() => import('@/pages/User/index.vue'))

// 按功能分割
export const DataVisualization = defineAsyncComponent(() => 
  import('@/components/business/DataVisualization.vue')
)

// 条件加载
export const AdminPanel = defineAsyncComponent(() => 
  import('@/pages/Admin/index.vue')
)
```

### 2. 错误处理架构

```vue
<!-- components/common/ErrorBoundary/index.vue -->

<template>
  <div v-if="hasError" class="error-boundary">
    <slot name="fallback" :error="error" :retry="retry">
      <el-result
        icon="error"
        title="页面出现错误"
        sub-title="抱歉，页面出现了意外错误，请刷新页面重试。"
      >
        <template #extra>
          <el-button type="primary" @click="retry">
            刷新页面
          </el-button>
        </template>
      </el-result>
    </slot>
  </div>
  <div v-else>
    <slot />
  </div>
</template>

<script setup lang="ts">
import { ref, onErrorCaptured } from 'vue'
import { ElResult, ElButton } from 'element-plus'

interface Props {
  onError?: (error: Error) => void
}

const props = defineProps<Props>()

const hasError = ref(false)
const error = ref<Error | null>(null)

// Vue 3 错误捕获
onErrorCaptured((err: Error) => {
  console.error('ErrorBoundary caught an error:', err)
  
  hasError.value = true
  error.value = err
  
  // 发送错误报告
  props.onError?.(err)
  
  // 错误监控服务
  if (import.meta.env.PROD) {
    // Sentry.captureException(err)
  }
  
  // 阻止错误继续传播
  return false
})

// 重试函数
const retry = () => {
  hasError.value = false
  error.value = null
  window.location.reload()
}
</script>

<style scoped>
.error-boundary {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 400px;
}
</style>
```

### 3. 微前端架构准备

```typescript
// config/micro-frontend.ts

// 微前端配置
export const microApps = [
  {
    name: 'user-management',
    entry: '//localhost:3001',
    container: '#user-management',
    activeRule: '/user'
  },
  {
    name: 'data-analytics',
    entry: '//localhost:3002',
    container: '#data-analytics',
    activeRule: '/analytics'
  }
]

// 全局状态共享
export const globalState = {
  token: '',
  userInfo: null,
  theme: 'light'
}

// 应用间通信
export const microAppCommunication = {
  // 发送消息到子应用
  sendMessage: (appName: string, data: any) => {
    window.postMessage({
      type: 'MICRO_APP_MESSAGE',
      target: appName,
      data
    }, '*')
  },
  
  // 监听子应用消息
  onMessage: (callback: (data: any) => void) => {
    window.addEventListener('message', (event) => {
      if (event.data.type === 'MICRO_APP_MESSAGE') {
        callback(event.data)
      }
    })
  }
}
```

---

**下一步**: 继续学习[性能优化](./性能优化.md)，了解AI如何帮助提升应用性能。