# 助教字段逻辑详解

## 概述

助教字段采用三字段设计模式，支持多选功能，确保前端操作、显示和后端传输的数据一致性：

- **`AssistantTeacherID`**: 表格绑定字段，存储逗号分隔的助教ID字符串，用于表格数据绑定和操作
- **`AssistantTeacherName`**: 显示字段，存储逗号分隔的助教姓名字符串，用于界面显示
- **`AssistantTeacherList`**: 传输字段，对象数组格式，用于后端API传输

## 与任课老师字段的关键差异

| 特性 | 任课老师（MainTeacher） | 助教（AssistantTeacher） |
|------|------------------------|--------------------------|
| **选择方式** | 单选 | 多选 |
| **ID字段格式** | 单个UUID字符串 | 逗号分隔的UUID字符串 |
| **名称字段格式** | 单个名称字符串 | 逗号和空格分隔的名称字符串 |
| **传输字段** | 单对象数组 `[{ID, Name}]` | 多对象数组 `[{ID, Name}, ...]` |
| **组件交互** | `modelValue: string` | `modelValue: string[]` |
| **数据转换** | 直接赋值 | 数组与字符串互转 |

## 字段定义

### 表格列配置
```typescript
{
    field: 'AssistantTeacherID',           // 绑定字段（逗号分隔字符串）
    key: 'AssistantTeacherID',             // 唯一标识
    title: '助教',                         // 列标题
    width: 115,
    renderBodyCell: createEditableCell(AssistantSelect, {
        // 组件配置...
    })
}
```

### 字段映射关系
```typescript
const fieldMap = {
    'AssistantTeacherID': 'AssistantTeacherName',  // ID字段 → 名称字段
    // ... 其他字段映射
}
```

### 数据格式示例
```typescript
// 前端表格数据格式
{
    AssistantTeacherID: "uuid1,uuid2,uuid3",                    // 逗号分隔字符串
    AssistantTeacherName: "张三, 李四, 王五",                     // 逗号空格分隔字符串
    AssistantTeacherList: [                                     // 对象数组
        {ID: "uuid1", Name: "张三"},
        {ID: "uuid2", Name: "李四"},
        {ID: "uuid3", Name: "王五"}
    ]
}
```

## 各种场景下的赋值逻辑

### 1. 用户手动选择助教（多选）

#### 触发时机
- 用户点击助教单元格
- 从多选下拉列表中选择助教

#### 赋值逻辑
```typescript
// 在 AssistantSelect 组件的 getProps 中
getProps: (row, column) => {
    let assistantIds = []
    let initialData = []
    
    // 优先使用 AssistantTeacherList 构建 initialData（最准确的数据源）
    if (row.AssistantTeacherList && Array.isArray(row.AssistantTeacherList) && row.AssistantTeacherList.length > 0) {
        initialData = row.AssistantTeacherList.map(assistant => ({
            id: assistant.ID,
            name: assistant.Name
        }))
        assistantIds = row.AssistantTeacherList.map(assistant => assistant.ID)
    } else {
        // 备用方案：从字符串字段构建
        assistantIds = row.AssistantTeacherID ? row.AssistantTeacherID.split(',').map(id => id.trim()).filter(Boolean) : []
        const assistantNames = row.AssistantTeacherName ? row.AssistantTeacherName.split(', ').map(name => name.trim()).filter(Boolean) : []
        
        initialData = assistantIds.map((id, idx) => ({
            id,
            name: assistantNames[idx] || id
        }))
    }
    
    return { 
        modelValue: assistantIds,  // 覆盖baseProps中的modelValue，传递数组而不是字符串
        initialData
    }
}

// 在 AssistantSelect 组件的 onChange 事件中
onChange: (val, assistantDataList) => {
    if (val && Array.isArray(val) && val.length > 0) {
        // 更新助教数据映射
        assistantDataList.forEach(assistantData => {
            if (assistantData && assistantData.id) {
                updateAssistantDataMap(assistantData.id, assistantData)
            }
        })
        
        // 更新显示名称字段（逗号空格分隔）
        row.AssistantTeacherName = assistantDataList.map(item => item.name).join(', ')
    } else {
        row.AssistantTeacherName = ''
    }
}

// 在 createEditableCell 的通用 onUpdate:modelValue 中
'onUpdate:modelValue': (val) => {
    const oldValue = row[column.field]
    
    // 特殊处理：助教字段需要将数组转换为字符串
    let finalValue = val
    if (column.field === 'AssistantTeacherID' && Array.isArray(val)) {
        finalValue = val.join(',')  // 数组转为逗号分隔字符串
    }
    
    row[column.field] = finalValue
    // ... 其他逻辑
}

// 在 onUpdateModelValue 中
onUpdateModelValue: (row, column, val) => {
    // 同步更新传输字段，使用最新的名称数据
    if (val && Array.isArray(val) && val.length > 0) {
        // 从 assistantDataMap 获取最准确的名称数据
        row.AssistantTeacherList = val.map(id => {
            const assistantData = assistantDataMap.value.get(id)
            return {
                ID: id,
                Name: assistantData ? assistantData.name : (id || '')
            }
        })
        
        // 确保 AssistantTeacherName 与 AssistantTeacherList 保持一致
        row.AssistantTeacherName = row.AssistantTeacherList.map(assistant => assistant.Name).join(', ')
    } else {
        row.AssistantTeacherList = []
        row.AssistantTeacherName = ''
    }
}
```

#### 作用
- 确保用户选择的多个助教ID和姓名正确关联
- 自动构建后端传输格式的 `AssistantTeacherList`
- 维护数组与字符串格式的同步

#### 后续操作
- 记录字段变更到变更收集器
- 触发自动保存倒计时

### 2. 新增行初始化

#### 触发时机
- 页面首次加载无草稿数据时
- 用户手动添加新行

#### 赋值逻辑
```typescript
function getNewRow(overrides) {
    return {
        // ... 其他字段
        AssistantTeacherID: '',              // 空字符串
        AssistantTeacherName: '',            // 空字符串
        AssistantTeacherList: [],            // 空数组
        // ... 其他字段
    }
}
```

#### 作用
- 为新行提供默认值
- 确保字段结构完整

#### 后续操作
- 新行会被标记为 `isEdit: true`
- 自动记录到变更收集器

### 3. 外部粘贴（从Excel等外部源）

#### 触发时机
- 用户从Excel复制数据
- 粘贴到表格中

#### 赋值逻辑
```typescript
// 在 beforePaste 中处理外部粘贴
Object.keys(dataRow).forEach(field => {
    if (field !== 'ID' && fieldMap[field]) {
        // 检验并格式化数据
        let result = fieldFormatCheck[field](dataRow[field])
        if (result !== false) {
            targetRow[field] = result
            // 记录字段变更
            recordFieldChange(targetRow.ID, field, result, 'paste')
            
            // 如果字段有映射，设置映射字段
            if (fieldMap[field] !== field) {
                targetRow[fieldMap[field]] = dataRow[field]
                // 记录映射字段变更
                recordFieldChange(targetRow.ID, fieldMap[field], dataRow[field], 'paste')
            }
        }
    }
})

// 同步更新到 tableData 中
const tableDataIndex = tableData.value.findIndex(row => row.ID === targetRow.ID)
if (tableDataIndex !== -1) {
    tableData.value[tableDataIndex] = { ...targetRow }
    
    // 同步更新助教传输字段
    if (targetRow.AssistantTeacherID && targetRow.AssistantTeacherName) {
        const assistantIds = targetRow.AssistantTeacherID.split(',').map(id => id.trim()).filter(Boolean)
        const assistantNames = targetRow.AssistantTeacherName.split(', ').map(name => name.trim()).filter(Boolean)
        targetRow.AssistantTeacherList = assistantIds.map((id, idx) => ({
            ID: id,
            Name: assistantNames[idx] || id
        }))
        // 记录 AssistantTeacherList 的变更
        recordFieldChange(targetRow.ID, 'AssistantTeacherList', targetRow.AssistantTeacherList, 'paste')
    } else {
        targetRow.AssistantTeacherList = []
        // 记录 AssistantTeacherList 的变更
        recordFieldChange(targetRow.ID, 'AssistantTeacherList', [], 'paste')
    }
}
```

#### 作用
- 处理外部数据格式
- 确保多个ID和名称字段同步
- 构建正确的传输字段

#### 后续操作
- 更新表格数据
- 记录所有相关字段变更
- 触发自动保存

### 4. 内部粘贴（表格内复制粘贴）

#### 触发时机
- 用户在表格内复制某行数据
- 粘贴到其他行

#### 赋值逻辑
```typescript
// 按照数据顺序，合并字段数据到目标行
copyData.value.forEach((dataRow, dataIndex) => {
    if (targetRows[dataIndex]) {
        const targetRow = targetRows[dataIndex]
        
        // 遍历要粘贴的字段，合并数据
        columnFields.forEach(field => {
            if (dataRow.hasOwnProperty(field)) {
                targetRow[field] = dataRow[field]
                // 记录字段变更
                recordFieldChange(targetRow.ID, field, dataRow[field], 'paste')
                
                // 如果复制的是ID字段，同时设置对应的名称字段
                if (fieldMap[field] && dataRow[fieldMap[field]]) {
                    targetRow[fieldMap[field]] = dataRow[fieldMap[field]]
                    // 记录映射字段变更
                    recordFieldChange(targetRow.ID, fieldMap[field], dataRow[fieldMap[field]], 'paste')
                }
            }
        })
        
        // 同步更新到 tableData 中
        const tableDataIndex = tableData.value.findIndex(row => row.ID === targetRow.ID)
        if (tableDataIndex !== -1) {
            tableData.value[tableDataIndex] = { ...targetRow }
            
            // 同步更新助教传输字段
            if (targetRow.AssistantTeacherID && targetRow.AssistantTeacherName) {
                const assistantIds = targetRow.AssistantTeacherID.split(',').map(id => id.trim()).filter(Boolean)
                const assistantNames = targetRow.AssistantTeacherName.split(', ').map(name => name.trim()).filter(Boolean)
                targetRow.AssistantTeacherList = assistantIds.map((id, idx) => ({
                    ID: id,
                    Name: assistantNames[idx] || id
                }))
                // 记录 AssistantTeacherList 的变更
                recordFieldChange(targetRow.ID, 'AssistantTeacherList', targetRow.AssistantTeacherList, 'paste')
            } else {
                targetRow.AssistantTeacherList = []
                // 记录 AssistantTeacherList 的变更
                recordFieldChange(targetRow.ID, 'AssistantTeacherList', [], 'paste')
            }
        }
    }
})
```

#### 作用
- 在表格内快速复制多选数据
- 保持字段映射关系
- 同步构建传输字段

#### 后续操作
- 显示成功提示
- 记录所有变更
- 触发自动保存

### 5. 自动填充

#### 触发时机
- 用户选择多行数据
- 点击自动填充按钮

#### 赋值逻辑
```typescript
// 遍历目标行，填充指定字段
targetRows.forEach(targetRow => {
    fieldsToCopy.forEach(field => {
        if (field !== 'ID') {
            targetRow[field] = sourceRowData[field]
            
            // 如果字段有映射，同时设置映射字段
            if (fieldMap[field] && fieldMap[field] !== field) {
                targetRow[fieldMap[field]] = sourceRowData[fieldMap[field]]
            }
        }
    })
    
    // 同步更新到 tableData 中
    const tableDataIndex = tableData.value.findIndex(row => row.ID === targetRow.ID)
    if (tableDataIndex !== -1) {
        tableData.value[tableDataIndex] = { ...targetRow }
        
        // 同步更新助教传输字段
        if (targetRow.AssistantTeacherID && targetRow.AssistantTeacherName) {
            const assistantIds = targetRow.AssistantTeacherID.split(',').map(id => id.trim()).filter(Boolean)
            const assistantNames = targetRow.AssistantTeacherName.split(', ').map(name => name.trim()).filter(Boolean)
            targetRow.AssistantTeacherList = assistantIds.map((id, idx) => ({
                ID: id,
                Name: assistantNames[idx] || id
            }))
            // 记录 AssistantTeacherList 的变更
            recordFieldChange(targetRow.ID, 'AssistantTeacherList', targetRow.AssistantTeacherList, 'autofill')
        } else {
            targetRow.AssistantTeacherList = []
            // 记录 AssistantTeacherList 的变更
            recordFieldChange(targetRow.ID, 'AssistantTeacherList', [], 'autofill')
        }
        
        // 记录字段变更
        fieldsToCopy.forEach(field => {
            if (field !== 'ID') {
                recordFieldChange(targetRow.ID, field, sourceRowData[field], 'autofill')
                
                // 如果字段有映射，同时记录映射字段
                if (fieldMap[field] && fieldMap[field] !== field) {
                    const mappedValue = targetRow[fieldMap[field]]
                    if (mappedValue !== undefined) {
                        recordFieldChange(targetRow.ID, fieldMap[field], mappedValue, 'autofill')
                    }
                }
            }
        })
    }
})
```

#### 作用
- 批量填充相同的多选数据
- 保持字段映射关系
- 自动构建传输字段

#### 后续操作
- 更新表格数据
- 记录所有变更
- 触发自动保存

### 6. 批量添加行（按规则分组）

#### 触发时机
- 用户选择分组字段为助教
- 点击批量添加按钮

#### 赋值逻辑
```typescript
// 在 handleAddTenRows 中
if (groupByField.value === 'AssistantTeacherID') {
    // 对于助教分组，groupKey 是逗号分隔的ID字符串
    newRow.AssistantTeacherID = groupKey
    
    // 从 assistantDataMap 构建名称字符串
    const assistantIds = groupKey.split(',').map(id => id.trim()).filter(Boolean)
    const assistantNames = assistantIds.map(id => {
        const assistantData = assistantDataMap.value.get(id)
        return assistantData ? assistantData.name : id
    })
    newRow.AssistantTeacherName = assistantNames.join(', ')
    
    // 同步传输字段
    if (groupKey) {
        newRow.AssistantTeacherList = assistantIds.map((id, idx) => ({
            ID: id,
            Name: assistantNames[idx] || id
        }))
    } else {
        newRow.AssistantTeacherList = []
    }
}
```

#### 作用
- 根据分组字段批量创建行
- 自动填充相关字段
- 构建正确的传输格式

#### 后续操作
- 添加到表格数据
- 记录所有变更
- 触发自动保存

### 7. 草稿数据加载

#### 触发时机
- 页面首次加载
- 用户切换表格类型

#### 赋值逻辑
```typescript
// 在 loadCourseDraftList 中
tableData.value = response.Data.List.map(draft => {
    // 处理助教数据：从 AssistantTeacherList 中提取 AssistantTeacherID 和 AssistantTeacherName
    const processedDraft = { ...draft }
    if (draft.AssistantTeacherList && Array.isArray(draft.AssistantTeacherList) && draft.AssistantTeacherList.length > 0) {
        const assistantIds = draft.AssistantTeacherList.map(assistant => assistant.ID || '').filter(Boolean)
        const assistantNames = draft.AssistantTeacherList.map(assistant => assistant.Name || '').filter(Boolean)
        processedDraft.AssistantTeacherID = assistantIds.join(',')          // 逗号分隔字符串
        processedDraft.AssistantTeacherName = assistantNames.join(', ')     // 逗号空格分隔字符串
    } else {
        // 如果没有 AssistantTeacherList，确保字段有默认值
        processedDraft.AssistantTeacherID = processedDraft.AssistantTeacherID || ''
        processedDraft.AssistantTeacherName = processedDraft.AssistantTeacherName || ''
        processedDraft.AssistantTeacherList = []
    }
    
    // 然后检测无效字段（现在 AssistantTeacherID 和 AssistantTeacherName 有值了）
    const errorFields = detectInvalidFields(processedDraft)
    
    // 为无效字段的名称补充默认值
    errorFields.forEach(idField => {
        const nameField = getFieldNameMapping(idField)
        if (nameField) {
            processedDraft[nameField] = '数据不存在'
        }
    })
    
    return {
        ...processedDraft,
        isEdit: false,
        isUpdatedFromDraft: true,
        errorField: errorFields
    }
})
```

#### 作用
- 从后端数据恢复前端字段
- 处理数据格式转换（数组→字符串）
- 检测无效数据

#### 后续操作
- 标记无效字段
- 补充默认名称
- 设置草稿状态

### 8. 草稿保存成功后的数据同步

#### 触发时机
- 草稿保存接口返回成功
- 处理 `SavedDraftList` 数据

#### 赋值逻辑
```typescript
// 在 saveDraft 中处理 SavedDraftList
response.Data.SavedDraftList.forEach(savedDraft => {
    const rowIndex = tableData.value.findIndex(row => row.ID === savedDraft.ID)
    if (rowIndex !== -1) {
        const updatedRow = {
            ...tableData.value[rowIndex],
            ...savedDraft,
        }
        
        // 处理助教数据：从 AssistantTeacherList 中提取 AssistantTeacherID 和 AssistantTeacherName
        if (savedDraft.AssistantTeacherList && Array.isArray(savedDraft.AssistantTeacherList) && savedDraft.AssistantTeacherList.length > 0) {
            const assistantIds = savedDraft.AssistantTeacherList.map(assistant => assistant.ID || '').filter(Boolean)
            const assistantNames = savedDraft.AssistantTeacherList.map(assistant => assistant.Name || '').filter(Boolean)
            updatedRow.AssistantTeacherID = assistantIds.join(',')
            updatedRow.AssistantTeacherName = assistantNames.join(', ')
            updatedRow.AssistantTeacherList = savedDraft.AssistantTeacherList
        }
        
        tableData.value[rowIndex] = updatedRow
    }
})
```

#### 作用
- 同步后端返回的最新数据
- 确保前后端数据一致
- 更新字段状态

#### 后续操作
- 更新表格显示
- 清除错误标记
- 重置编辑状态

### 9. 错误字段处理

#### 触发时机
- 草稿保存失败
- 字段验证不通过

#### 赋值逻辑
```typescript
// 在 saveDraft 中处理 SaveErrorList
response.Data.SaveErrorList.forEach(error => {
    const rowIndex = tableData.value.findIndex(row => row.ID === error.DraftId)
    if (rowIndex !== -1) {
        const existingErrors = tableData.value[rowIndex].errorField || []
        
        // 映射后端字段名到前端字段名
        const mappedErrors = newErrors.map(fieldName => {
            // AssistantTeacherList 映射为 AssistantTeacherID
            if (fieldName === 'AssistantTeacherList') {
                return 'AssistantTeacherID'
            }
            return fieldName
        })
        
        const mergedErrors = [...new Set([...existingErrors, ...mappedErrors])]
        tableData.value[rowIndex].errorField = mergedErrors
    }
})
```

#### 作用
- 标记无效字段
- 映射字段名错误
- 合并错误信息

#### 后续操作
- 显示错误样式
- 提供错误提示
- 允许用户修正

### 10. 错误字段点击清理

#### 触发时机
- 用户点击标记为错误的单元格
- 自动清空相关值

#### 赋值逻辑
```typescript
// 在 eventCustomOption.bodyCellEvents.click 中
if (hasError) {
    // 清空当前字段值
    if (column.field === 'AssistantTeacherID') {
        // 助教字段：ID设置为空字符串，名称设置为空字符串，传输字段设置为空数组
        row[column.field] = ''
        row.AssistantTeacherList = []
    } else {
        // 其他字段：ID设置为空字符串，名称设置为空字符串
        row[column.field] = ''
    }
    
    // 记录字段变更
    recordFieldChange(row.ID, column.field, row[column.field], 'error_clear')
    
    // 清空对应的名称字段
    const nameField = fieldMap[column.field]
    if (nameField) {
        row[nameField] = ''
        recordFieldChange(row.ID, nameField, row[nameField], 'error_clear')
    }
    
    // 清空错误标记
    row.errorField = row.errorField.filter(field => field !== column.field)
}
```

#### 作用
- 清空无效数据
- 重置字段状态
- 清除错误标记

#### 后续操作
- 更新表格显示
- 记录清理操作
- 触发自动保存

### 11. Delete键删除单元格

#### 触发时机
- 用户选中单元格，按Delete键
- 表格组件执行删除操作

#### 赋值逻辑
```typescript
// 在 afterDelete 回调中
if (selectionRangeIndexes && displayTableData.value) {
    // 获取删除的行范围
    const startRowIndex = selectionRangeIndexes.startRowIndex
    const endRowIndex = selectionRangeIndexes.endRowIndex
    const colIndex = selectionRangeIndexes.startColIndex

    // 根据列索引找到对应的字段名
    const column = columns[colIndex]
    if (column && column.field) {
        // 遍历删除的行，清理相关字段
        for (let rowIndex = startRowIndex; rowIndex <= endRowIndex; rowIndex++) {
            const targetRow = displayTableData.value[rowIndex]
            if (targetRow && !targetRow.isGroupRow && !targetRow.isGroupFooter) {
                // 清理被删除字段的相关名称
                clearRelatedFields(targetRow, column.field)
                
                // 记录字段变更到变更收集器
                recordFieldChange(targetRow.ID, column.field, '', 'delete')
                
                // 同时记录相关名称字段的变更
                const nameField = fieldMap[column.field]
                if (nameField) {
                    recordFieldChange(targetRow.ID, nameField, '', 'delete')
                }
                
                // 特殊处理：如果是助教字段，记录 AssistantTeacherList 的变更
                if (column.field === 'AssistantTeacherID') {
                    recordFieldChange(targetRow.ID, 'AssistantTeacherList', [], 'delete')
                }
                
                // 同步到 tableData.value
                const tableDataIndex = tableData.value.findIndex(row => row.ID === targetRow.ID)
                if (tableDataIndex !== -1) {
                    tableData.value[tableDataIndex] = { ...targetRow }
                }
            }
        }
    }
}
```

#### 作用
- 清空被删除字段的值
- 清理相关名称字段
- 记录所有字段变更

#### 后续操作
- 更新表格数据
- 记录所有变更
- 触发自动保存

### 12. 剪切操作（Ctrl+X）

#### 触发时机
- 用户选中单元格或区域，按Ctrl+X
- 表格组件执行剪切操作

#### 赋值逻辑

##### 剪切前（beforeCut）
```typescript
beforeCut: ({ data, selectionRangeIndexes, selectionRangeKeys }) => {
    // 分组不支持剪切
    if (selectionRangeKeys.startRowKey.startsWith('group_')) {
        return false
    }
    
    // 复用通用的数据收集逻辑
    collectDataForClipboard(selectionRangeIndexes, true) // true表示剪切操作
    return true
}

// 通用数据收集函数
const collectDataForClipboard = (selectionRangeIndexes, isCut) => {
    const columnFields = columns.map(v => v.field).filter((_, index) => {
        return index >= selectionRangeIndexes.startColIndex && index <= selectionRangeIndexes.endColIndex
    });
    
    copyData.value = displayTableData.value.filter((_, index) => {
        return index >= selectionRangeIndexes.startRowIndex && index <= selectionRangeIndexes.endRowIndex
    }).map(row => {
        const clipboardRow = { ID: row.ID }
        columnFields.filter(v => v).forEach(field => {
            // 收集ID字段和对应的Name字段
            clipboardRow[field] = row[field]
            clipboardRow[fieldMap[field]] = row[fieldMap[field]]
        })
        return clipboardRow
    })
}
```

##### 剪切后（afterCut）
```typescript
afterCut: ({ data, selectionRangeIndexes, selectionRangeKeys }) => {
    // 清理剪切后的原字段值，并记录变更
    if (selectionRangeIndexes && displayTableData.value) {
        // 获取剪切的行列范围
        const startRowIndex = selectionRangeIndexes.startRowIndex
        const endRowIndex = selectionRangeIndexes.endRowIndex
        const startColIndex = selectionRangeIndexes.startColIndex
        const endColIndex = selectionRangeIndexes.endColIndex

        // 遍历剪切的区域
        for (let rowIndex = startRowIndex; rowIndex <= endRowIndex; rowIndex++) {
            const targetRow = displayTableData.value[rowIndex]
            if (targetRow && !targetRow.isGroupRow && !targetRow.isGroupFooter) {
                
                // 遍历剪切的列
                for (let colIndex = startColIndex; colIndex <= endColIndex; colIndex++) {
                    const column = columns[colIndex]
                    if (column && column.field) {
                        // 清理被剪切字段的相关名称
                        clearRelatedFields(targetRow, column.field)
                        
                        // 记录字段变更到变更收集器
                        recordFieldChange(targetRow.ID, column.field, '', 'cut')
                        
                        // 同时记录相关名称字段的变更
                        const nameField = fieldMap[column.field]
                        if (nameField) {
                            recordFieldChange(targetRow.ID, nameField, '', 'cut')
                        }
                        
                        // 特殊处理：如果是助教字段，记录 AssistantTeacherList 的变更
                        if (column.field === 'AssistantTeacherID') {
                            recordFieldChange(targetRow.ID, 'AssistantTeacherList', [], 'cut')
                        }
                    }
                }
                
                // 同步到 tableData.value
                const tableDataIndex = tableData.value.findIndex(row => row.ID === targetRow.ID)
                if (tableDataIndex !== -1) {
                    tableData.value[tableDataIndex] = { ...targetRow }
                }
            }
        }
    }
}
```

#### 作用
- 收集剪切区域的所有字段数据（包括ID和Name）
- 清空原字段值
- 记录所有字段变更
- 支持多行多列剪切

#### 后续操作
- 数据存储在copyData.value中
- 原字段被清空并记录变更
- 可以粘贴到其他位置
- 触发自动保存

#### 与复制的区别
- **复制**：不清除原字段，只收集数据
- **剪切**：清除原字段，收集数据，记录变更
- **粘贴逻辑**：完全一致，都使用copyData.value中的数据

## 数据传输流程

### 前端 → 后端
1. **数据收集**: `collectAllChanges()` 收集所有变更
2. **字段构建**: 自动构建 `AssistantTeacherList` 传输字段
   ```typescript
   // 新增行和修改行都会构建传输字段
   if (tableRow.AssistantTeacherID || tableRow.AssistantTeacherName) {
       const originalAssistantIds = tableRow.AssistantTeacherID ? tableRow.AssistantTeacherID.split(',').map(id => id.trim()).filter(Boolean) : []
       const assistantNames = tableRow.AssistantTeacherName ? tableRow.AssistantTeacherName.split(', ').map(name => name.trim()).filter(Boolean) : []
       
       // 构建助教列表，保留所有名称，但验证ID
       const maxLength = Math.max(originalAssistantIds.length, assistantNames.length)
       rowData.AssistantTeacherList = []
       
       for (let i = 0; i < maxLength; i++) {
           const originalId = originalAssistantIds[i] || ''
           const name = assistantNames[i] || originalId || ''
           
           // 验证ID是否为有效UUID，无效则设为空字符串
           const validatedId = originalId && isUUID(originalId) ? originalId : ''
           
           if (name) { // 只要有名称就保留这个助教
               rowData.AssistantTeacherList.push({
                   ID: validatedId, // 无效UUID时为空字符串
                   Name: name
               })
           }
       }
   } else {
       rowData.AssistantTeacherList = []
   }
   ```
3. **UUID验证**: 验证每个 `AssistantTeacherID` 格式，无效ID设为空字符串
4. **API调用**: 调用 `BatchSaveCourseDraft` 接口

### 后端 → 前端
1. **草稿查询**: `GetCourseDraftList` 返回 `AssistantTeacherList`
2. **数据同步**: 提取 `AssistantTeacherID` 和 `AssistantTeacherName`
3. **保存响应**: 处理 `SavedDraftList` 和 `SaveErrorList`
4. **错误处理**: 映射字段错误并标记

## 关键函数说明

### `updateAssistantDataMap(assistantId, assistantData)`
- **作用**: 缓存助教数据到全局映射表
- **参数**: 助教ID、助教数据对象
- **触发**: 用户选择助教时

### `validateIDField(fieldName, value)` 对于 AssistantTeacherID
- **作用**: 验证逗号分隔的UUID字符串
- **处理**: 分割字符串，过滤无效UUID，返回有效UUID的逗号分隔字符串
- **返回**: 有效UUID的逗号分隔字符串或空字符串
```typescript
if (fieldName === 'AssistantTeacherID') {
    if (typeof value === 'string') {
        const ids = value.split(',').map(id => id.trim()).filter(Boolean)
        const validUUIDs = ids.filter(id => isUUID(id))
        return validUUIDs.join(',')
    } else {
        return ''
    }
}
```

### `isUUID(value)` 直接验证
- **作用**: 验证单个字符串是否为有效UUID格式
- **应用**: 在构建传输字段时逐个验证ID
- **优势**: 保留名称，仅将无效ID设为空字符串

### `detectInvalidFields(draft)` 对于助教字段
- **作用**: 检测无效的关联数据
- **逻辑**: 检查 `AssistantTeacherID` 字段是否有对应的名称值
- **返回**: 无效字段名数组

## 注意事项

1. **字段同步**: 任何时候修改 `AssistantTeacherID` 或 `AssistantTeacherName`，都要同步更新 `AssistantTeacherList`

2. **数据格式转换**: 
   - 组件交互：数组格式 `string[]`
   - 表格绑定：逗号分隔字符串 `string`
   - 显示名称：逗号空格分隔字符串 `string`
   - 后端传输：对象数组 `{ID, Name}[]`

3. **变更记录**: 所有字段修改都要调用 `recordFieldChange` 记录变更

4. **错误映射**: 后端 `AssistantTeacherList` 错误要映射到前端 `AssistantTeacherID` 字段

5. **数据一致性**: 确保前端三个字段的数据始终保持一致

6. **UUID验证**: 传输前验证ID字段格式，无效ID设为空字符串但保留名称

7. **优先数据源**: 优先使用 `AssistantTeacherList` 构建组件初始数据，确保数据准确性

## 总结

助教字段采用三字段设计，支持多选功能，通过完善的赋值逻辑和同步机制，确保了：
- 前端操作的便利性（ID字段绑定，名称字段显示，支持多选）
- 数据的一致性（三个字段自动同步，格式正确转换）
- 传输的准确性（自动构建后端格式，保留名称但验证ID）
- 错误的可追踪性（完整的变更记录和错误处理）

这种设计模式在任课老师字段的基础上，增加了多选支持和复杂的数据格式转换，为其他类似的多选字段（如学员等）的实现提供了参考模板。 