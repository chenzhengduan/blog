# 已完成
4934
1. 可见列计算
当前多处手写过滤隐藏列/预检查列。
抽成：getVisibleColumns({ exclude = ['preCheckStatus'] })，统一用于复制/剪切/删除/自动填充。

# 已完成
科目字段的特殊处理
在“外部粘贴、内部复制粘贴、自动填充”的三处，对 SubjectID/SubjectName 的非全科行清空、全科行双字段同步逻辑几乎相同。
抽成：applySubjectChange(row, { id, name }, source)。

# 待完成
教师/助教传输字段构建
多处根据 MainTeacherID/Name 和 AssistantTeacherID/Name 重建 MainTeacherList/AssistantTeacherList。
抽成：buildMainTeacherList(row)、buildAssistantList(row)，并在 paste/autofill/edit 后统一调用。

# 待完成
变更记录与关联名称同步
多处先改值、再 recordFieldChange，并顺带同步映射字段（ID→Name 或 Name→ID）。
抽成：setFieldWithMirror(row, field, value, source)，内部处理映射和 recordFieldChange。

# 待完成
timeRange 与 StartTime/EndTime 同步
在编辑/粘贴/剪切/删除时均有拆分/清空逻辑。
抽成：syncTimeRange(row, timeRange, source)。

# 待完成
错误字段与提示清理
多处在值变化/清空时清理 row.errorField、row.errorMessages、必填校验缓存。
抽成：clearErrorForField(row, field)、clearValidationForField(rowId, field)。

# 待完成
容量上限提示与截断
新增行入口与后端载入都在计算 remaining 并给出提示文本。
抽成：enforceCapacityAndWarn({ incomingCount, actionLabel }) 返回允许数量与统一提示。

# 待完成
预检查字段命中与样式判定
在单元格渲染和样式回调处重复“字段别名匹配/是否命中限制/冲突”的判断。
抽成：isPrecheckHit(rowId, field)、getFieldAliases(field)（已存在类似，建议集中到 util）。

# 待完成
复制/粘贴数据收集
两处针对列范围的相同计算、以及 ID/Name 成对收集。
抽成：collectClipboardData(range, rows, columns)，内部已使用 getVisibleColumns。

# 待完成
分组缓存失效与展开控制
多处改动数据后标记 groupedDataCache.value.isValid = false。
抽成：invalidateGroupCache()，并在需要时统一调用。